import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text style={{ fontSize: 20, fontWeight: "bold" }}>Rides</Text>
    </SafeAreaView>
  );
};

export default Rides;
